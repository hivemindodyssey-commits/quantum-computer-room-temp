# Data Directory

Place raw and processed datasets here.

## Suggested Layout
- `raw/` for unmodified instrument exports
- `processed/` for cleaned/normalized data
- `samples/` for small example datasets

## Notes
- Do not overwrite raw files.
- Use timestamped filenames for traceability.
- Keep a short changelog for processing steps.
